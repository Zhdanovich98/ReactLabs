let initialState = {
    firstname: "",
    lastname: "",
    email: ""
}

const usersReducers = (state = initialState, action) =>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}
export default handleSubmitAC = () => ({

})
export default usersReducers;